'use-strict';

var expect = require('chai').expect;

describe('User', function () {
   describe('POST', function () {
        it('Should gives true', function () {
            expect(true).to.be.true;
        });
   });
});